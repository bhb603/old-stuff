require 'spec_helper'

describe Award do
  
  it "fails validation without a job"
  it "fails validation without a recipient"
  it "can only belong to a valid recipient"
  it "can only belong to a job with no other awards"

end
